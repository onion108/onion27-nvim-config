# My Neovim Configuration

My own neovim configuration. Don't guarantee this will work on your machine, but feel free to put it on your machine.
Requires at least `neovim 0.12`. Also requires `tree-sitter`, `ripgrep`, `lazygit` and `fd` (a `find` replacement, not the file descriptor). And a bunch of language servers that even I can't remember.

~You may want to change some absolute paths in the configuration. One I can remember is in the `coc-settings.json`.~
> No long CoC! Already moved to nvim-lsp.

## Why the commit messages are in a mess
Don't fucking care about it. It's my personal repository for backup and I'm not going to maintain it for running on everyone's computer. I'm not against with someone cloning it and try to run it but I just don't care about that so I'll not make beautiful and readable commit messages. It's not like, a "serious" open source project after all. It's just my personal backup.

